apiVersion: v1
kind: Service
metadata:
  name: ci-demo-svc
  labels:
    app: ci-demo
spec:
  type: LoadBalancer
  selector:
    app: ci-demo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
